# URL pública del backend (sin barra final). En local apunta a Docker/uvicorn;
# en Vercel poné la URL del backend de Railway, ej: https://mi-back.up.railway.app
NEXT_PUBLIC_API_URL=http://localhost:8000
